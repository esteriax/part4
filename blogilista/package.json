{
  "name": "blogapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "test": "NODE_ENV=test node --test --test-concurrency=1",
    "git": "git add . && git commit -m \"$npm_config_message\" && git push origin main",
    "lint": "eslint ."
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^16.0.0",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "eslint": "^10.4.0",
    "nodemon": "^3.0.0",
    "supertest": "^7.2.2"
  }
}
